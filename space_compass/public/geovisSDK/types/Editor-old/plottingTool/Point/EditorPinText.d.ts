import EditorPin from './EditorPin';
/**
  * @private
  */
export default class EditorPinText extends EditorPin {
    pinBuilder: Cesium.PinBuilder;
    constructor(p: any, viewer: any, layer: any);
    initProps(defs: any): void;
    initShape(): void;
}
