import EditorPin from './EditorPin';
/**
  * @private
  */
export default class EditorPinImage extends EditorPin {
    constructor(p: any, viewer: any, layer: any);
    initProps(defs: any): void;
    initShape(): void;
}
